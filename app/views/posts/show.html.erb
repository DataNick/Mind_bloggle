
	<h1>
		<%= @post.title %>
	</h1>
	<p>
		Submitted <%= time_ago_in_words(@post.created_at) %> ago
	</p>
	<p>
		<%= @post.body %>
	</p>

	<%= link_to "Edit", edit_post_path %> | <%= link_to "Delete", post_path(@post), method: :delete %>

<h2>Comments</h2>
	<%= render "comments/comment" %>

<h2>Leave a comment</h2>
	<%= render "comments/form" %>